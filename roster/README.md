# roster/

Put real HR roster CSVs and org-specific config here. Everything in this folder
except this README is git-ignored, because it contains personal information.

See `fixtures/demo_roster.csv` and `fixtures/demo_config.json` for the format.
